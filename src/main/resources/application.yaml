spring:
  application:
    name: UserServices

  datasource:
    url: jdbc:postgresql://${DATABASE_URL}
    username: ${DATABASE_USERNAME}
    password: ${DATABASE_PASSWORD}

    driver-class-name: org.postgresql.Driver

jwt:
  public:
    key: file:${FILE_PUBKEY}
  private:
    key: file:${FILE_PRIVKEY}
